# Tax-Planning

Tax planning strategies and guides.

The `guides/` folder holds the Tax Strategies reference document (`.docx` and `.html`), deployed live via Cloudflare Workers static assets from this repo's `main` branch.
